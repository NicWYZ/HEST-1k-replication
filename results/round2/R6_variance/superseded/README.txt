Superseded outputs of stage R6_variance.

byte-identical to r6_donor_variance_components.csv apart from one fewer written significant digit.

Kept rather than deleted so a citation that breaks is recoverable by path. Nothing here should be read as a current result.
